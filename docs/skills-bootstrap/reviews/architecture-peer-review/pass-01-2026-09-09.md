# docs/skills-bootstrap/reviews/architecture-peer-review/pass-01-2026-09-09.md

# Architecture peer review — pass 1

**Plan folder:** `docs/skills-bootstrap/`

**Date:** 2026-09-09

**Scope:** full

**Reviewer:** architecture-peer-review skill

**Baseline:** [SKILLS_BOOTSTRAP_FINDINGS.md](../../SKILLS_BOOTSTRAP_FINDINGS.md), [SKILLS_BOOTSTRAP_GENERAL_PLAN.md](../../SKILLS_BOOTSTRAP_GENERAL_PLAN.md)

Cited paths checked against the workspace and `~/projects/agent-workflow` @ `d0f8027`.

## Verdict

Review complete: 0 critical, 0 high, 4 medium, 2 low.

**BLOCK create-execution-plan:** no — git hazard, leftover catalog, skill audit, and hosting/stack adapt are locked; LOOP vs idle SSOT is in findings Q14 and general-plan cross-cutting.

## Prior pass

None.

## Rollup (critical / high)

| Severity | Section | Title |
|:---|:---|:---|
| — | — | none |

## Cross-cutting — locked table / hosting / pack

| Severity | Area | Finding |
|:---|:---|:---|
| medium | execution contract | P2 still packs many deliverables (catalog, manifest, flows, SSOT, all skills, `docs/agents/`, `AGENTS.md`, `BUGBOT.md`, `.cursorrules`). Execution must stay within 3–6 subphases or split P2. |
| medium | general plan | Pack verify checklist (idle-looking SSOT) conflicts with Q14 until P3. Execution P2 should verify pack files exist; P3 owns idle `active_program: null`. |
| low | findings | Pack `create-findings` still names `docs/DATABASE_CONNECTION_GUIDE.md` — parked, not a consumer block. |

**Verified OK:**

- Q1–Q14 present; Q2 `none` vs live `origin` kp-platform is explicit leftover, not hosting.
- `pack_source.path` already `../agent-workflow` in `.agent/manifest.json`; `project_name` still `kp-platform` (**verified**).
- `.revy/review-context.json` still `ur-etl`; no `.agent/review-context.json`.
- Pack `patterns/ADAPT.md` / `skills-audit.md` match the audit table (Revy skip, planning on, docs export on).

## P0 — Git safety

| Severity | Area | Finding |
|:---|:---|:---|
| medium | execution | “Prefer orphan / `git init`” is two mechanics. Execution must pick **one** and keep `docs/skills-bootstrap/` + `game-platform-base-plan.md` in the new history. |
| low | general plan | Local `main` still tracks `origin/main` of kp-platform. Orphan/init should not leave a pushable kp `main` checkout as the default mental model — execution note only. |

**Verified OK:** origin is `git@github.com:raimondskrauklis/kp-platform.git`; branch `feat/ur-etl-weekly-ckan`. `.gitignore` is deleted in the working tree. P0 before P1 is correct.

## P1 — Strip leftovers

| Severity | Area | Finding |
|:---|:---|:---|
| medium | findings / P1 | After deleting `.revy/`, there is no review SSOT until P2. Acceptable in one LOOP; execution must not stop overnight between P1 and P2. |

**Verified OK:** `code-review/` has `agent-workflow-pack`, `starter-pack`, `product/revy` (**verified** `code-review/README.md`). `.github/workflows/deploy.yml` is `CI/CD - kp-platform`. Skill dir listing includes corpus, line-count, `lv-en-project-planning`, `babysit-revy-pr`, `sentry-fix-issues`. `.cursor/rules/` includes `git-quick.md` and always-apply `sentry-mcp.mdc` (no `.cursor/mcp.json`).

## P2 — Materialize pack and adapt

| Severity | Area | Finding |
|:---|:---|:---|
| — | — | no high/critical |

**Verified OK:** P2 copies the audit **install** set (not “remaining kp skills”). Review-context is this LOOP (Q14), not idle, not `ur-etl`. Manifest shape matches `project.manifest.template.json` + planned `apps/api` / `apps/web` / `apps/game-unity`. `test_commands` empty is required by ADAPT (no lockfiles).

## P3 — Verify

| Severity | Area | Finding |
|:---|:---|:---|
| — | — | no high/critical |

**Verified OK:** P3 walks findings verification 1–7 and returns idle SSOT. Out of scope correctly excludes game scaffold and pack GitHub remote.

## Open questions

None blocking. Execution may choose orphan vs `git init` without changing locked direction (Q3: detach + new history).
