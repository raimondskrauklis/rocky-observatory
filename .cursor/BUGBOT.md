# Bugbot — review contract

**Active program:** none (`idle`)

**SSOT:** [.agent/review-context.json](../.agent/review-context.json)

Default review authority: [.cursorrules](../.cursorrules) and [AGENTS.md](../AGENTS.md).

When a LOOP is running, set `active_program` and `programs[].scope` in the SSOT, then point this file at that program’s findings, general plan, and current execution file.
